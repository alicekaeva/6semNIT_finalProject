<form method="post" action="{{ path('app_article_delete', {'id': article.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить эту запись?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}"><br>
    <button class="btn">Удалить</button>
</form>
