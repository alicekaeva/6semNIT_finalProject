{% extends 'base.html.twig' %}

{% block title %}Автор{% endblock %}

{% block body %}
    <h1>Автор</h1>

    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>ФИО</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
        {% for author in authors %}
            <tr>
                <td>{{ author.id }}</td>
                <td>{{ author.name }}</td>
                <td>
                    <a href="{{ path('app_author_show', {'id': author.id}) }}">Показать</a>
                    <a href="{{ path('app_author_edit', {'id': author.id}) }}">Редактировать</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Записей не найдено</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_author_new') }}">Создать новую запись</a>
{% endblock %}
