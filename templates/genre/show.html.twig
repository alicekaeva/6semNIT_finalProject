{% extends 'base.html.twig' %}

{% block title %}Жанр{% endblock %}

{% block body %}
    <h1>Жанр</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Название</th>
                <td>{{ genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_genre_index') }}">Назад к списку</a>

    <a href="{{ path('app_genre_edit', {'id': genre.id}) }}">Редактировать</a>

    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
